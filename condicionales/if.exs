#variable que define la edad
edad = 18
#condicion
if edad >= 18
#inicio de bloque do-end
do
  IO.puts("mayor de edad")#Expresion A (indica lo que pasa si es verdadero )

else #else señala lo que pasa si la expresion A no se cumple

IO.puts("menor de edad")#Exprecion B (indica lo que pasa si es falso )
end
#fin del bloque do-end

#podemos ver a los bloques como un separador
#que indica donde termina la condicion y donde
#empiezan y terminan las expresiones.
